name: Cypress Test
on:
  push:
    branches: [master]
  pull_request:
    branches: 
    - '**'
    - '!desarrollo'
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2   
      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          start: npm start   
      - name: Build Jenkins job
        uses: GoldenspearLLC/build-jenkins-job@1.0.0
        with:
          jenkins-url: "http://jk.toctoc.com"
          jenkins-token: "117673c0629e6073950ee567fe9c424bcb"
          user: "mhurtado"        
          job-path: "e2e_ui_test"






        
      
      
        

