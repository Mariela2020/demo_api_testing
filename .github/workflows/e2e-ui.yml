name: Cypress Test
on:
  push:
    branches: [master]
  pull_request:
    branches: 
    - '**'
    - '!desarrollo'
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2   
      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          start: npm start   
      - name: Jenkins Job Trigger
        uses: estherk0/jenkins-trigger@1.0
        with:
          url: "http://localhost:8080"
          username: "mhurtado"
          api_token: "111d12bdfd6c06e3bb9f075780ecd01d35"
          job_name: "e2e_test"
          parameter: "SuperSecret"
          wait: true
          timeout: 600






        
      
      
        

